using UnityEditor;

namespace Vault {
    public static class VaultAssetDatabase {
        // public static void OpenInEditor(object ToOpen) {
        //     foreach (var lAssetPath in AssetDatabase.GetAllAssetPaths()) {
        //         if (lAssetPath.EndsWith(ToOpen.GetType().Name+".cs")) {
        //             var lScript = (MonoScript)AssetDatabase.LoadAssetAtPath(lAssetPath, typeof(MonoScript));
        //             if (lScript != null) {
        //                 AssetDatabase.OpenAsset(lScript);
        //                 break;
        //             }
        //         }
        //     }
        // }
    }
}